export type ICost = {
    cost: string;
    value: number;
    time: string;
    typeCost: string;
    isActive: boolean;

}

/*
    import { collection, addDoc, getDocs, query, where, doc, updateDoc, deleteDoc } from "firebase/firestore"
*/